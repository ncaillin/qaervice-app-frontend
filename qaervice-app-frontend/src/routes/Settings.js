import './Settings.css';
import HomeWrapper from '../components/HomeWrapper';

const Settings = () => {
  return(
    <HomeWrapper pos={4} breadcrumb={'Settings'}></HomeWrapper>
  );
};

export default Settings;
